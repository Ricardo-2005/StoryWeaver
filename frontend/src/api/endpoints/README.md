# Endpoint modules

Endpoint wrappers for Auth, Project, Snapshot, Canon Assets, Characters, Worldbook, Outlines, Chapters and Skills live here. They call the shared `apiClient` and use types derived from the actual backend Controller/DTO contract documented in `docs/api-contract.md`.
