# Phase 0 范围记录

已实现：Vue、Vite、TypeScript 严格模式、Router、Pinia、TanStack Query、Element Plus、Design Token、主题、ESLint、Vitest、Playwright、多阶段 Dockerfile、Nginx、Compose 与前端 CI。

明确未实现：登录、注册、项目列表/新建/详情、App Shell、会话侧边栏、正典资产业务页面、SSE、章节工作流、世界书检索与 DeepSeek 功能。这些属于 Phase 1 及以后，不能用 Mock 或本地假数据冒充后端能力。

仓库盘点时没有根目录、`backend` 或 `frontend` 的 `AGENTS.md`；`frontend` 目录也不存在，因此本阶段从空目录创建。
