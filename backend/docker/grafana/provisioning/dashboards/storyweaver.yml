apiVersion: 1

providers:
  - name: StoryWeaver
    orgId: 1
    folder: StoryWeaver
    type: file
    disableDeletion: true
    editable: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
