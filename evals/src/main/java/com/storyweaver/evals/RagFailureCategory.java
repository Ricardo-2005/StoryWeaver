package com.storyweaver.evals;

enum RagFailureCategory {
    CONSTANT_RULE_CROWDING,
    MULTI_ASSET_MISSING,
    SEMANTIC_MISMATCH,
    KEYWORD_MISMATCH,
    DUPLICATE_RESULT,
    WRONG_ASSET_TYPE,
    SCOPE_FILTER_ERROR,
    TOKEN_BUDGET_TRUNCATION,
    RELEVANT_BUT_LOW_RANK,
    TRUE_RETRIEVAL_MISS,
    OTHER
}
